spring:
  application:
    name: product-service

  datasource:
    username: postgres
    password: password
    driver-class-name=org:
      postgresql:
        Driver:
    url: jdbc:postgresql://localhost:5432/mydb

  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect:
              PostgreSQLDialect:

server:
  port: 8999
